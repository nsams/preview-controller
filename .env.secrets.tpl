# Copy to .env.secrets, which is not committed.

# Shared password for the status page, the api and every preview
PREVIEW_CONTROLLER_PASSWORD=""

# Optional, only needed for private repositories. A fine-grained personal access token with
# read access to the contents of the repositories that should be previewable.
PREVIEW_CONTROLLER_GITHUB_TOKEN=""
